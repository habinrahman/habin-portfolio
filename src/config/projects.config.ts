/**
 * @deprecated Import from `portfolio.config` or `./portfolio.projects.build` instead.
 * Kept so existing `../data/projects` re-exports keep working.
 */
export type { PortfolioProjectConfig } from './portfolio.projects.build'
export { certificateLiveVerificationProof, PROJECTS, PROJECT_BY_ID } from './portfolio.projects.build'
